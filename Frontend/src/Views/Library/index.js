import React from "react";

// Components
import Dashboard from "../../Components/Common/Dashboard";

const Library = (props) => {
  return (
    <Dashboard
      config={[]}
    />
  );
};

export default Library;
